function dist = lr_dist(s,t)

ns = size(s,1);
nt = size(t,1);

dist = 0;
limit = 4;

for i=1:nt
    
    best_dist = inf;
    left_bound = min(max(0,i-limit);
    right_bound = min(ns,i+limit);
        
    dist = dist  + best_dist;
end

end
